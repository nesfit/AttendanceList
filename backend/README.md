- Založeno na NodeJS s využitím knihovny Express 
- Lokální server se spustí příkazem `npm start`
- Obsahuje i mock WIS api, který se spustí `npm run mock` (musí se nastavit správné cesty v src/config.js)
